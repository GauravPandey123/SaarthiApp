package com.saarthiapp.android.ui.home

import android.view.View
import android.view.ViewGroup
import android.widget.ImageView
import android.widget.TextView
import androidx.constraintlayout.widget.ConstraintLayout
import com.saarthiapp.android.R

class CustomMarkerView(root: ViewGroup,
                       text: String?,
                       isSelected: Boolean) : ConstraintLayout(root.context) {
   /* private var mImage: ImageView
    private var mTitle: TextView*/

   /* init {
        View.inflate(context, R.layout.custom_marker_layout, this)
        mImage = findViewById(R.id.imgFeedImageUser)
        mTitle = findViewById(R.id.tvPostImageUsername)
        measure(View.MeasureSpec.UNSPECIFIED, View.MeasureSpec.UNSPECIFIED)
        mTitle.text = text
       *//* if (isSelected) {
            mImage.setImageResource(R.drawable.ic_marker_selected)
        } else {
            mImage.setImageResource(R.drawable.ic_marker_not_selected)
        }*//*
    }*/
}